<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jesus.waiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="waiapp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="waiapp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GameDescription" />
        <activity android:name=".SearchActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="waiapp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginMailActivity" />
        <activity android:name=".AccountActivity" />
        <activity
            android:name=".AddGameSearchPhase"
            android:label="waiapp" />
        <activity android:name=".AddGameAddPhase" />
        <activity android:name=".AlquilerListaActivity" />
        <activity android:name=".ProfileRentActivity" />
        <activity
            android:name=".ChatActivity"
            android:label="waiapp"
            android:theme="@style/ChatTheme" />
    </application>

</manifest>